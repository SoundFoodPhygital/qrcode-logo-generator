[project]
name = "A CLI qr-code generator with logo support"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "qrcode[pil]>=8.2",
]
